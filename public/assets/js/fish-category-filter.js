(() => {
  'use strict';

  // Shared fish catalogue state. fish-catalog.js reads these values when
  // applying the condition and category filters.
  window.condition = 'all';
  window.category = 'all';
})();

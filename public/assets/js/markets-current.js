(()=>{'use strict';const root=document.querySelector('.markets-current');if(!root)return;const stage=root.querySelector('.current-stage'),line=root.querySelector('.current-line path'),dot=root.querySelector('.current-dot'),moves=[...root.querySelectorAll('.current-movement')];let target=0,shown=0;const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));const update=()=>{if(!stage)return;const rect=stage.getBoundingClientRect(),max=Math.max(1,stage.offsetHeight-window.innerHeight),p=clamp(-rect.top/max,0,1);target=p;shown+=(target-shown)*.1;const len=2600;if(line)line.style.strokeDashoffset=String(len-(len*shown));if(dot){const y=18+shown*64;dot.style.top=y+'vh';dot.style.left=(7+shown*18)+'vw'};const idx=Math.min(moves.length-1,Math.floor(shown*moves.length));moves.forEach((m,i)=>m.classList.toggle('active',i===idx));root.style.setProperty('--current-progress',shown);root.style.setProperty('--current-hue',String(shown*18))};window.addEventListener('scroll',update,{passive:true});window.addEventListener('resize',update,{passive:true});root.addEventListener('pointermove',e=>{const r=root.getBoundingClientRect();root.style.setProperty('--mx',(e.clientX-r.left)/r.width);root.style.setProperty('--my',(e.clientY-r.top)/r.height)});update()})();

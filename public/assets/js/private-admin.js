(()=>{'use strict';const keyInput=document.querySelector('#adminKey'),unlock=document.querySelector('#unlockAdmin'),authStatus=document.querySelector('#adminAuthStatus'),panel=document.querySelector('#adminPanel');if(!keyInput||!unlock||!panel)return;let adminKey='';const set=(el,msg,ok=false)=>{if(!el)return;el.textContent=msg;el.style.color=ok?'#b9d9bd':'#d8bb7a'};const call=async(url,body)=>{const r=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${adminKey}`,Accept:'application/json','content-type':'application/json'},body:JSON.stringify(body)});const type=r.headers.get('content-type')||'';const data=type.includes('application/json')?await r.json():{};if(!r.ok||!data.ok)throw new Error(data.error||`Request failed (${r.status})`);return data};const unlockAdmin=async()=>{const candidate=keyInput.value.trim();if(!candidate){set(authStatus,'Enter your admin key.');return}adminKey=candidate;set(authStatus,'Checking access…');try{await call('/api/private/admin/approve',{email:'__access_check__',name:'',company:'__access_check__',language:'en'});set(authStatus,'Admin access verified.',true);panel.hidden=false;keyInput.value='';keyInput.disabled=true;unlock.disabled=true}catch(e){if(e.message==='email and company required'){set(authStatus,'Admin access verified.',true);panel.hidden=false;keyInput.value='';keyInput.disabled=true;unlock.disabled=true;return}adminKey='';set(authStatus,e.message)}};unlock.addEventListener('click',unlockAdmin);keyInput.addEventListener('keydown',e=>{if(e.key==='Enter')unlockAdmin()});const bind=(selector,handler)=>{const form=document.querySelector(selector);if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button[type="submit"]'),status=form.querySelector('[data-status]');if(btn){btn.disabled=true;btn.setAttribute('aria-busy','true')}set(status,'');try{const message=await handler(form);set(status,message||'Done.',true);if(message!==false)form.reset()}catch(err){set(status,err?.message||'Request failed.')}finally{if(btn){btn.disabled=false;btn.removeAttribute('aria-busy')}}})};bind('[data-client-form]',async form=>{const fd=new FormData(form);await call('/api/private/admin/approve',{email:String(fd.get('email')||'').trim().toLowerCase(),name:String(fd.get('name')||'').trim(),company:String(fd.get('company')||'').trim(),language:String(fd.get('language')||'en')});return'Client approved.'});bind('[data-offer-form]',async form=>{const fd=new FormData(form),dt=String(fd.get('valid_until')||''),ts=dt?Math.floor(new Date(dt).getTime()/1000):0;if(!ts)throw new Error('Choose a valid expiry date.');await call('/api/private/admin/offers',{title:String(fd.get('title')||'').trim(),category:String(fd.get('category')||'').trim(),origin:String(fd.get('origin')||'').trim(),destination:String(fd.get('destination')||'').trim(),availability:String(fd.get('availability')||'').trim(),valid_until:ts,description:String(fd.get('description')||'').trim()});return'Private offer published.'});bind('[data-send-offer-form]',async form=>{const fd=new FormData(form);const id=Number(fd.get('offer_id'));if(!Number.isInteger(id)||id<1)throw new Error('Enter a valid offer ID.');const d=await call('/api/private/admin/send-offer',{offer_id:id});return`Sent to ${d.sent||0} approved client(s).`});bind('[data-newsletter-form]',async form=>{const fd=new FormData(form);const d=await call('/api/admin/newsletter/send',{subject:String(fd.get('subject')||'').trim(),html:String(fd.get('html')||''),language:String(fd.get('language')||'')});return`Market Signals sent to ${d.sent||0} subscriber(s).`});})();

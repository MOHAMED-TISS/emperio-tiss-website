(()=>{'use strict';const host=document.querySelector('#private-offers'),status=document.querySelector('#private-status');if(!host)return;const set=(t,ok=false)=>{if(status){status.textContent=t;status.style.color=ok?'#d7bb7d':'#fff'}};const load=async()=>{try{const r=await fetch('/api/private/offers',{credentials:'include',headers:{Accept:'application/json'}}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok){set('Private access is not active for this session. Use the access request on News.');return}set(`Access verified for ${d.client}`,true);const offers=d.offers||[];host.innerHTML=offers.length?offers.map((o,i)=>`<article class="news-card"><span class="news-card-index">${String(i+1).padStart(2,'0')}</span><span class="news-tag">${o.category||'PRIVATE OFFER'}</span><h3>${o.title}</h3><p><strong>Origin:</strong> ${o.origin||'—'}<br><strong>Destination:</strong> ${o.destination||'—'}<br><strong>Availability:</strong> ${o.availability||'—'}</p><p>${o.description||''}</p><a href="/en/contact/">Discuss this offer ↗</a></article>`).join(''):'<p>No active private offers at the moment.</p>'}catch(e){set('Unable to load private offers.')}};load()})();
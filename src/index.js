const ALLOWED_ORIGINS=new Set(['https://emperio-tiss.com','https://www.emperio-tiss.com']);const RESEND='https://api.resend.com/emails';const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'}});const clean=(v,n=4000)=>String(v??'').trim().slice(0,n);const emailOk=v=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v);const originOk=r=>{const o=r.headers.get('origin');return o&&ALLOWED_ORIGINS.has(o)};const dbOk=e=>e&&e.NEWS_DB;const now=()=>Math.floor(Date.now()/1000);const enc=new TextEncoder();async function sha(v){const b=await crypto.subtle.digest('SHA-256',enc.encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}function token(){return crypto.randomUUID().replaceAll('-','')+crypto.randomUUID().replaceAll('-','')}async function resend(env,to,subject,html,replyTo){if(!env.RESEND_API_KEY)throw new Error('RESEND_API_KEY_MISSING');const r=await fetch(RESEND,{method:'POST',headers:{authorization:`Bearer ${env.RESEND_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({from:'EMPERIO TISS <no-reply@emperio-tiss.com>',to:[to],reply_to:replyTo,subject,html})});if(!r.ok)throw new Error(`RESEND_${r.status}`)}async function authAdmin(r,env){return env.ADMIN_API_KEY&&r.headers.get('authorization')===`Bearer ${env.ADMIN_API_KEY}`}async function confirmNewsletter(request,env){const raw=clean(new URL(request.url).searchParams.get('token'),200),hash=await sha(raw);if(!dbOk(env)||!raw)return new Response('Invalid confirmation link.',{status:400});const row=await env.NEWS_DB.prepare("SELECT email FROM auth_tokens WHERE token_hash=? AND type='newsletter' AND expires_at>?").bind(hash,now()).first();if(!row)return new Response('This confirmation link is invalid or expired.',{status:400});await env.NEWS_DB.batch([env.NEWS_DB.prepare("UPDATE subscribers SET confirmed_at=? WHERE email=?").bind(now(),row.email),env.NEWS_DB.prepare("DELETE FROM auth_tokens WHERE token_hash=?").bind(hash)]);return new Response('<!doctype html><meta charset="utf-8"><title>EMPERIO TISS</title><body style="font-family:Arial;background:#f4efe5;padding:60px;color:#173028"><h1>You’re in.</h1><p>Your Market Signals subscription is confirmed.</p><p><a href="/news/">Back to News</a></p></body>')}async function subscribe(request,env){if(!originOk(request))return json({ok:false,error:'Origen no autorizado.'},403);if(!dbOk(env))return json({ok:false,error:'Newsletter service is being configured.'},503);const f=await request.formData(),email=clean(f.get('email'),254).toLowerCase(),language=['es','en','fr','it','ar'].includes(clean(f.get('language'),2))?clean(f.get('language'),2):'en';if(clean(f.get('_honey'),200))return json({ok:false,error:'Solicitud rechazada.'},400);if(!emailOk(email)||!f.get('consent'))return json({ok:false,error:'Introduce un email válido y acepta las comunicaciones.'},400);const raw=token(),hash=await sha(raw),exp=now()+86400;await env.NEWS_DB.prepare("INSERT INTO subscribers(email,language,consent_at,confirmed_at) VALUES(?,?,?,NULL) ON CONFLICT(email) DO UPDATE SET language=excluded.language,consent_at=excluded.consent_at,confirmed_at=NULL").bind(email,language,now()).run();await env.NEWS_DB.prepare("INSERT OR REPLACE INTO auth_tokens(token_hash,type,email,expires_at) VALUES(?,?,?,?)").bind(hash,'newsletter',email,exp).run();const link=`https://emperio-tiss.com/api/newsletter/confirm?token=${encodeURIComponent(raw)}`;try{await resend(env,email,'Confirm your EMPERIO TISS Market Signals subscription',`<p>Thank you for subscribing to EMPERIO TISS Market Signals.</p><p><a href="${link}">Confirm subscription</a></p><p>This link expires in 24 hours.</p>`)}catch(e){return json({ok:false,error:'No se pudo enviar el email de confirmación.'},502)}return json({ok:true,message:'Revisa tu email para confirmar la suscripción.'})}async function requestAccess(request,env){if(!originOk(request))return json({ok:false,error:'Origen no autorizado.'},403);if(!dbOk(env))return json({ok:false,error:'Private access is being configured.'},503);const f=await request.formData(),email=clean(f.get('email'),254).toLowerCase();if(clean(f.get('_honey'),200))return json({ok:false,error:'Solicitud rechazada.'},400);if(!emailOk(email))return json({ok:false,error:'Introduce un email válido.'},400);let client=await env.NEWS_DB.prepare("SELECT email,company,status FROM clients WHERE email=?").bind(email).first();if(!client){await env.NEWS_DB.prepare("INSERT INTO clients(email,status,created_at) VALUES(?,'pending',?)").bind(email,now()).run();try{await resend(env,'info@emperio-tiss.com','Private access request — EMPERIO TISS',`<p>Pending private-client registration: <strong>${email}</strong></p>`)}catch{}return json({ok:true,message:'Solicitud recibida. Nuestro equipo revisará tu acceso.'})}if(client.status!=='approved')return json({ok:true,message:'Tu acceso está pendiente de aprobación.'});const raw=token(),hash=await sha(raw),exp=now()+1800;await env.NEWS_DB.prepare("INSERT OR REPLACE INTO auth_tokens(token_hash,type,email,expires_at) VALUES(?,?,?,?)").bind(hash,'private',email,exp).run();const link=`https://emperio-tiss.com/api/private/verify?token=${encodeURIComponent(raw)}`;try{await resend(env,email,'Your EMPERIO PRIVATE access link',`<p>Your EMPERIO PRIVATE access is ready.</p><p><a href="${link}">Enter EMPERIO PRIVATE</a></p><p>This link expires in 30 minutes.</p>`)}catch(e){return json({ok:false,error:'No se pudo enviar el enlace privado.'},502)}return json({ok:true,message:'Te hemos enviado un enlace de acceso privado.'})}async function privateVerify(request,env){const raw=clean(new URL(request.url).searchParams.get('token'),200),hash=await sha(raw),row=await env.NEWS_DB?.prepare("SELECT email FROM auth_tokens WHERE token_hash=? AND type='private' AND expires_at>?").bind(hash,now()).first();if(!row)return new Response('Invalid or expired access link.',{status:400});const sessionRaw=token(),sessionHash=await sha(sessionRaw),exp=now()+604800;await env.NEWS_DB.batch([env.NEWS_DB.prepare("INSERT INTO private_sessions(token_hash,email,expires_at) VALUES(?,?,?)").bind(sessionHash,row.email,exp),env.NEWS_DB.prepare("DELETE FROM auth_tokens WHERE token_hash=?").bind(hash)]);return new Response(null,{status:302,headers:{location:'/private/','set-cookie':`et_private_session=${sessionRaw}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=604800`}})}async function privateOffers(request,env){const cookie=request.headers.get('cookie')||'',m=cookie.match(/(?:^|;\s*)et_private_session=([^;]+)/);if(!m||!dbOk(env))return json({ok:false,error:'Unauthorized'},401);const hash=await sha(m[1]),s=await env.NEWS_DB.prepare("SELECT email FROM private_sessions WHERE token_hash=? AND expires_at>?").bind(hash,now()).first();if(!s)return json({ok:false,error:'Unauthorized'},401);const rows=await env.NEWS_DB.prepare("SELECT id,title,category,origin,destination,availability,valid_until,description,created_at FROM private_offers WHERE status='published' AND valid_until>? ORDER BY created_at DESC").bind(now()).all();return json({ok:true,client:s.email,offers:rows.results||[]})}async function adminApprove(request,env){if(!(await authAdmin(request,env)))return json({ok:false,error:'Unauthorized'},401);if(!dbOk(env))return json({ok:false,error:'NEWS_DB missing'},503);const b=await request.json(),email=clean(b.email,254).toLowerCase(),company=clean(b.company,160),name=clean(b.name,160),language=clean(b.language,2)||'en';if(!emailOk(email)||!company)return json({ok:false,error:'email and company required'},400);await env.NEWS_DB.prepare("INSERT INTO clients(email,name,company,language,status,created_at,approved_at) VALUES(?,?,?,?, 'approved',?,?) ON CONFLICT(email) DO UPDATE SET name=excluded.name,company=excluded.company,language=excluded.language,status='approved',approved_at=excluded.approved_at").bind(email,name,company,language,now(),now()).run();return json({ok:true})}async function adminOffer(request,env){if(!(await authAdmin(request,env)))return json({ok:false,error:'Unauthorized'},401);if(!dbOk(env))return json({ok:false,error:'NEWS_DB missing'},503);const b=await request.json();await env.NEWS_DB.prepare("INSERT INTO private_offers(title,category,origin,destination,availability,valid_until,description,status,created_at) VALUES(?,?,?,?,?,?,?,'published',?)").bind(clean(b.title,200),clean(b.category,80),clean(b.origin,120),clean(b.destination,120),clean(b.availability,120),Number(b.valid_until),clean(b.description,2000),now()).run();return json({ok:true})}async function adminSendOffer(request,env){if(!(await authAdmin(request,env)))return json({ok:false,error:'Unauthorized'},401);if(!dbOk(env))return json({ok:false,error:'NEWS_DB missing'},503);const b=await request.json(),id=Number(b.offer_id),offer=await env.NEWS_DB.prepare("SELECT * FROM private_offers WHERE id=?").bind(id).first();if(!offer)return json({ok:false,error:'Offer not found'},404);const clients=await env.NEWS_DB.prepare("SELECT email FROM clients WHERE status='approved'").all();let sent=0;for(const c of clients.results||[]){try{await resend(env,c.email,`EMPERIO PRIVATE — ${offer.title}`,`<h2>${offer.title}</h2><p><strong>${offer.category}</strong></p><p>Origin: ${offer.origin}<br>Destination: ${offer.destination}<br>Availability: ${offer.availability}</p><p>${offer.description||''}</p><p><a href="https://emperio-tiss.com/private/">View EMPERIO PRIVATE</a></p>`);sent++}catch{}}await env.NEWS_DB.prepare("UPDATE private_offers SET last_sent_at=? WHERE id=?").bind(now(),id).run();return json({ok:true,sent})}async function adminNewsletter(request,env){if(!(await authAdmin(request,env)))return json({ok:false,error:'Unauthorized'},401);if(!dbOk(env))return json({ok:false,error:'NEWS_DB missing'},503);const b=await request.json(),subject=clean(b.subject,200),html=clean(b.html,20000),language=clean(b.language,2);if(!subject||!html)return json({ok:false,error:'subject and html required'},400);const rows=language&&['es','en','fr','it','ar'].includes(language)?await env.NEWS_DB.prepare("SELECT email FROM subscribers WHERE confirmed_at IS NOT NULL AND language=?").bind(language).all():await env.NEWS_DB.prepare("SELECT email FROM subscribers WHERE confirmed_at IS NOT NULL").all();let sent=0;for(const c of rows.results||[]){try{await resend(env,c.email,subject,html);sent++}catch{}}return json({ok:true,sent})}const escapeHtml=value=>clean(value).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');async function handleContact(request,env){if(!originOk(request))return json({ok:false,error:'Origen no autorizado.'},403);const form=await request.formData();if(clean(form.get('_honey'),200))return json({ok:false,error:'Solicitud rechazada.'},400);const nombre=clean(form.get('nombre'),120),empresa=clean(form.get('empresa'),160),email=clean(form.get('email'),254),telefono=clean(form.get('telefono'),80),producto=clean(form.get('producto'),120),destino=clean(form.get('destino'),160),mensaje=clean(form.get('mensaje'),4000);if(!nombre||!empresa||!email||!telefono||!producto||!destino||!mensaje)return json({ok:false,error:'Completa todos los campos obligatorios.',code:'VALIDATION_FAILED'},400);if(!emailOk(email))return json({ok:false,error:'Introduce un email válido.',code:'INVALID_EMAIL'},400);const html=`<h2>Nueva consulta B2B — EMPERIO TISS</h2><p><strong>Nombre:</strong> ${escapeHtml(nombre)}</p><p><strong>Empresa:</strong> ${escapeHtml(empresa)}</p><p><strong>Email:</strong> ${escapeHtml(email)}</p><p><strong>Teléfono:</strong> ${escapeHtml(telefono)}</p><p><strong>Producto:</strong> ${escapeHtml(producto)}</p><p><strong>Destino:</strong> ${escapeHtml(destino)}</p><p><strong>Necesidad:</strong></p><p>${escapeHtml(mensaje).replaceAll('\n','<br>')}</p>`;try{await resend(env,'info@emperio-tiss.com',`Nueva consulta B2B — ${empresa} — ${producto}`,html,email)}catch(e){return json({ok:false,error:'No se pudo enviar la consulta. Inténtalo de nuevo.'},502)}return json({ok:true})}async function fetchHandler(request,env){const url=new URL(request.url);if(url.pathname==='/api/newsletter/subscribe'&&request.method==='POST')return subscribe(request,env);if(url.pathname==='/api/newsletter/confirm'&&request.method==='GET')return confirmNewsletter(request,env);if(url.pathname==='/api/private/request-access'&&request.method==='POST')return requestAccess(request,env);if(url.pathname==='/api/private/verify'&&request.method==='GET')return privateVerify(request,env);if(url.pathname==='/api/private/offers'&&request.method==='GET')return privateOffers(request,env);if(url.pathname==='/api/private/admin/approve'&&request.method==='POST')return adminApprove(request,env);if(url.pathname==='/api/private/admin/offers'&&request.method==='POST')return adminOffer(request,env);if(url.pathname==='/api/private/admin/send-offer'&&request.method==='POST')return adminSendOffer(request,env);if(url.pathname==='/api/admin/newsletter/send'&&request.method==='POST')return adminNewsletter(request,env);if(url.pathname==='/api/contact'&&request.method==='POST')return handleContact(request,env);return env.ASSETS.fetch(request)}export default{async fetch(request,env){try{return await fetchHandler(request,env)}catch(e){return json({ok:false,error:'Request could not be completed.',detail:String(e?.message||e).slice(0,200)},500)}}};